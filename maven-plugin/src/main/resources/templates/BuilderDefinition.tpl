	/**
	 * Returns a new connection handler for a given connection for version {{vitf.version}} of the interface.
	 * @param connection the connection used to create the new connection handler
	 * @return the connection handler
	 */
	public {{vitf.handler.interface}} build{{vitf.version}}(Connection connection);